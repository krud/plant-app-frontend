import SwiftUI

struct Slogan: View {
    var body: some View {
        return Text("Explore the world of Plants")
        .font(.subheadline)
        .padding(EdgeInsets(top: 0, leading: 0, bottom: 50, trailing: 0))
    }
}

