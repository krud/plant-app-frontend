import SwiftUI

struct BackToLanding: View {
    
    var body: some View {
        return HStack(alignment: .top){
                Text("< Back")
                    .foregroundColor(.black)
                    .bold()
//            Spacer()
        }
    }
}
